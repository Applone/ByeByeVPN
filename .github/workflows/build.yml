name: Deprecated Build Workflow

on:
  workflow_dispatch:

jobs:
  moved:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow moved
        run: |
          echo "This workflow has been replaced by .github/workflows/debug.yml and .github/workflows/release.yml"
